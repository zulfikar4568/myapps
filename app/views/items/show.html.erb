<div class="container">
    <div class="row">
        <div class="col-sm-7 mt-4">
            <div class="card p-3">
                <p class="h5"><%= @item.name%></p>
                <p class="mt-2">Price: $<%= @item.price %></p>
                    <%= simple_format @item.description%>
            </div>
        </div>
        <div class=col-sm-1>
        </div>
        <div class="col-sm-4">
            <%= image_tag "posts.svg", class: "img-fluid"%>
        </div>
    </div>
</div>